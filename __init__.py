"""Music Mashup — automatic series splicing.

A Python engine + web UI for splicing a *series/collection* of songs into a
single continuous mix: optionally detect cut points (silence / beats), apply
crossfades, gain, and loudness normalization — while still allowing the user to
set explicit in/out points.
"""

__version__ = "0.2.0"
